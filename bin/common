#!/bin/bash
##################################################
set -ue
BIN_DIR=$(cd "$(dirname "${BASH_SOURCE:-$0}")"; pwd)
##################################################

chmod +x "$BIN_DIR"/*
common(){
  cd "$BIN_DIR/.."
  ./gradlew build
}

common