#!/bin/bash
##################################################
set -ue
BIN_DIR=$(cd "$(dirname "${BASH_SOURCE:-$0}")"; pwd)
##################################################

bash "$BIN_DIR"/common

main() {
  java -jar "$BIN_DIR/../kinfra-cli/build/libs/kinfra-cli-dev.jar" "$@"
}
main "$@"

